Actor := &
	data': X'
	messages: Array { .' | .' }

Actor = &
	new: { data :X' | :Actor(X')
		; create the event loop
		'{}
		Actor. data []
	}
	
	do: { self!, message :{ .' | .' } |
		; notify that there is a new message
		'{}
		self.messages.append message
	}
